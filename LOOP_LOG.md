# Loop Log

One entry per task attempt. Append-only.

Format: `## YYYY-MM-DDTHH:MM:SSZ — Task <id> [<status>]`
- Agent ID:
- Summary (1 paragraph):
- Branch / commit:

---

## 2026-06-25T00:48:00Z — Bootstrap [info]
- Orchestrator initialized.
- Repo had no `.git`; ran `git init` locally. No remote configured (push step deferred — see IMPROVEMENTS).
- Plan: split Task 1 across 3 subagents (1a FastAPI backend, 1b Vite scaffold + data fetch, 1c charts + sweep UI). Then 7 → 2 → 3 → 4 → 5 → 6.

## 2026-06-25T09:00:00Z — Task 1a [success]
- Summary: Built `api/` (FastAPI + Pydantic v2) exposing `/health`, `/tickers`, `/strategies`, `/backtest`, `/sweep`. Reuses the existing `backtest` engine, `sweep` harness, and `strategies.REGISTRY` — no logic reimplemented. Services layer keeps HTTP concerns separate; numpy/NaN scrubbing lives in `_jsonable`. CORS opened to `:5173` and `:3000`. Tests under `tests/api/test_endpoints.py` use `TestClient`; data-backed cases run against the local ClickHouse (all 7 pass, 12 total in repo). Requirements appended with fastapi / uvicorn[standard] / pydantic>=2 / httpx.
- Commit: 30b3d9b
- Cut for scope: no tmp-schema fixture for ClickHouse tests (skip-when-unreachable instead); no LRU cache for repeated bar loads in sweep; no streaming/pagination on `/sweep` results; per-ticker metric slice falls back to `None` for vbt scalar returns. All logged in IMPROVEMENTS.md.

## 2026-06-25T09:30:00Z — Task 1b [success]
- Summary: Scaffolded `frontend/` with Vite 5 + React 18 + TypeScript (pinned create-vite@5 because Node 20.11 predates create-vite@9's engines floor). Added TanStack Query, axios, and Tailwind v3. `api/client.ts` wraps `/health`, `/tickers`, `/strategies`, `/backtest`, `/sweep` against `VITE_API_URL` (default `http://localhost:8000`); types in `api/types.ts` hand-mirror `api/schemas.py`. `Sidebar.tsx` fetches tickers + strategies, renders a checkbox multi-select, date pickers, strategy dropdown, an auto-generated params form prefilled from `default_params` (number inputs where the default is numeric), and a Single/Sweep radio (Sweep is a stub for 1c). The Run button POSTs `/backtest`, logs to console, and routes the response into `Main.tsx` which renders it inside a `<pre>`. `npm run build` succeeds (236 KB JS / 8 KB CSS, gzipped 77/2.3 KB); `npm run dev` boots on :5173. README documents quickstart.
- Commit: a38ab9e
- Cut for scope: no Vitest/Playwright smoke test (deferred to 1c); no shared TS schema codegen (hand-maintained types); no charts, metrics grid, or sweep results UI (1c); ticker selector is a flat checkbox list rather than a search combobox; param form has no validation against `param_grid`. All logged in IMPROVEMENTS.md.

## 2026-06-25T01:05:27Z — Task 1c [success]
- Summary: Replaced the Task 1b `<pre>` dump with the real research UI. Added `MetricsTable` (TanStack Table v8 — per-ticker Sharpe/return/drawdown/win rate with sortable columns), `EquityChart` (Recharts `LineChart`, one line per ticker on a shared time X-axis), and `PriceChart` (per-ticker line + up/down triangle markers at entry/exit timestamps, with a ticker pill selector when >1 ticker). The Sidebar's Sweep mode now POSTs `/sweep` and renders a `SweepTable` (params + sharpe/return/dd/win/n_trades/exposure) plus `SharpeHeatmap` (HTML-table heatmap with HSL colour ramp) when exactly 2 params are swept — otherwise a "needs exactly 2" note. The sweep grid editor in the sidebar lets the user toggle individual values per `param_grid` key (default = all). Added `scripts/dev.sh` (bash, traps SIGINT, polls because macOS bash lacks `wait -n`) and a root `Makefile` (`make dev` / `make test` / `make build`). Deprecated `dashboard/app.py` with a top-of-file comment block and rewrote the README "Stack" + "Quickstart" + the old `## 4. Launch the dashboard` section. Vitest 2 + jsdom 25 + MSW 2 wired up (downgraded from Vitest 4 because Node 20.11.1 in this env lacks `node:util#styleText`); one smoke test at `frontend/src/__tests__/Sidebar.test.tsx` mounts Sidebar with mocked `/tickers` + `/strategies` and asserts the params form renders — passes. `make dev` was booted end-to-end in the sandbox: both servers come up cleanly (FastAPI on :8000, Vite on :5173) and shut down on SIGTERM. `pytest -q` still 12/12; `npm run build` succeeds (693 KB JS / 209 KB gzip — Recharts is the bulk).
- Commit: 3fc52f6
- Cut for scope: PriceChart plots equity-curve rather than raw close price (no `/price` endpoint exists); heatmap is plain HTML, no interpolation; marker tooltip uses Recharts default (no entry-vs-exit label); no Playwright e2e; one smoke test only (no Run-button / sweep-flow coverage); bundle is one 692 KB chunk (no code-splitting); Vitest pinned to v2 because of Node 20.11 / rolldown engine gap. All logged in IMPROVEMENTS.md.

## 2026-06-25T10:30:00Z — Task 7a [success]
- Summary: Built the first slice of the evaluation gauntlet — benchmarks and regime tagging. `backtest/benchmarks.py` exposes `buy_and_hold(prices_wide, weights='equal'|'cap', caps=...)`, `buy_and_hold_spy(start, end, interval)` (loads from ClickHouse, falls back to a YFinance backfill via the existing `data.backfill_ticker` on a miss — no fetch logic re-implemented), and `random_entry_monte_carlo(n_paths=500, exposure_target=0.5, seed=42)` returning a `(n_paths × n_bars)` numpy equity matrix plus a polars per-path summary. `backtest/regimes.py` gives `tag_trend` (SPY vs 200d SMA), `tag_volatility` (window-relative VIX terciles), `tag_drawdown` (calm/mild/severe at -5%/-15% from rolling 1y high), and a `tag_all` combiner. `^VIX` is just a yfinance symbol — no special-casing needed, documented inline. `backtest/regime_split.py` implements `split_stats_by_regime(returns, regimes_df)` returning per-regime Sharpe / total return / max DD / exposure. Wired `POST /benchmarks` into FastAPI (`api/app.py`, `api/schemas.py`, `api/services.py`) reusing `_jsonable`. Tests: 17 new under `tests/backtest/` (synthetic prices, hand-crafted SPY/VIX series, hand-computed regime Sharpe), 2 new in `tests/api/test_endpoints.py` (one CH-gated). Full suite: 31 passed, 0 failed.
- Commit: 610271a
- Cut for scope: no /regimes endpoint (UI agent can call client-side or we add later); no auto-VIX-backfill helper (caller must run `python scripts/backfill.py --tickers ^VIX`); random MC uses i.i.d. Bernoulli (no block autocorrelation); `regime_split.exposure` uses a non-zero-return proxy rather than reading vbt's `asset_value()`; drawdown thresholds are hand-picked (calibration deferred); SPY backfill in /benchmarks is synchronous (can block first request). All logged in IMPROVEMENTS.md.

## 2026-06-25T12:30:00Z — Task 7c [success]
- Summary: Built the OOS + attribution leg of the gauntlet. `backtest/walkforward.py` adds `WalkForwardConfig` / `FoldResult` dataclasses, `walk_forward(strategy_cls, prices_wide, param_grid, config, ...)` (both expanding and rolling modes, `step` defaults to `test_size` for non-overlapping folds, `min_train` clips the first expanding fold's lookback), and `aggregate_walkforward(folds)` returning `{oos_sharpe_distribution, oos_sharpe_mean, oos_sharpe_ci, is_vs_oos, decay_slope, n_folds}` — the OOS Sharpe CI reuses `stats.bootstrap_ci`; the decay slope is plain OLS of OOS~IS Sharpes (≪1 → overfit). No signal logic is re-implemented; each fold delegates to `sweep` on the train slice and `run_backtest` on the test slice. `backtest/attribution.py` adds `market_attribution` (CAPM OLS via `np.linalg.lstsq` returning alpha / beta / alpha_t_stat / r_squared / residual_returns / systematic_returns) and `child_signal_attribution` (ticker-agnostic Σ w·r decomposition with a `residual` key for any gap; scalar weights are broadcast). Wired `POST /walkforward` and `POST /attribution` into FastAPI (schemas + services + routes). 18 new backtest tests (expanding vs rolling fold sizing, `min_train` clipping, IS/OOS gap detection on noise, decay-slope hand calc, attribution recovers β=0.7 on hand-built data, perfect-fit R²=1, 2-child sum check) + 3 new API tests (attribution is synthetic-only and never skips; walk-forward is CH-gated). Full suite: 77 passed.
- Commit: d53c1c6
- Cut for scope: no purged k-fold / embargo per López de Prado (chronological splits only); best-combo picker is greedy max-Sharpe with no DSR deflation; `decay_slope` uses plain OLS (no Theil-Sen / paired bootstrap on slope); `_portfolio_returns` averages per-ticker equity instead of vbt's composite; no Newey-West / HAC SE on alpha t-stat (plain OLS only); single-factor CAPM (no FF / Carhart); child-signal attribution decomposes return only, not variance; `/walkforward` runs synchronously inside the request. All logged in IMPROVEMENTS.md.

## 2026-06-25T11:30:00Z — Task 7b [success]
- Summary: Built `backtest/stats.py` — the statistical core of Task 7's evaluation gauntlet. `probabilistic_sharpe_ratio` faithfully implements Bailey & López de Prado (2012) eq. (4) with raw-4th-moment kurtosis (not excess) and a per-period SR̂ derived from an explicit `periods_per_year` argument. `deflated_sharpe_ratio` (Bailey & López de Prado 2014) uses the Euler-Mascheroni expected-max-of-N-normals approximation inline with comments; `deflated_sharpe_ratio_from_sweep` derives `n_trials` and `sr_trials_std` from a sweep array. `bootstrap_ci` is the Politis & Romano (1994) **stationary bootstrap** — geometric block-length, wrap-around indexing — with `sharpe_ci` / `total_return_ci` / `max_drawdown_ci` wrappers. `spa_test` shipped as **White's (2000) Reality Check** with re-centring at the sample mean, NOT the full Hansen (2005) SPA — flagged in docstring + IMPROVEMENTS. Helper metrics (`annualised_sharpe`, `annualised_return`, `max_drawdown`) accept np.ndarray or pl.Series. `POST /stats` exposes Sharpe / PSR / max-DD / total-return with bootstrap CIs (pure compute, no DB). 23 new tests in `tests/backtest/test_stats.py` (reproducibility, DSR n=1/std=0 == PSR boundary, true-SR=1 coverage at 99%, SPA detection + null case) + 2 API tests. Full suite: 56 passed.
- Commit: af4c1ff
- Cut for scope: White's Reality Check instead of full Hansen SPA (re-centring rule for negative competitors not implemented); no HAC / Newey-West parametric Sharpe CI; bootstrap CI is percentile (not BCa); DSR assumes independent sweep trials (no effective-N correction for correlated grid neighbours); `/stats` recomputes three bootstrap batches (no resample-once-apply-many). All logged in IMPROVEMENTS.md.

## 2026-06-25T20:15:00Z — Task 7e [success]
- Summary: Built `reports/` — a standalone Jinja2 + matplotlib HTML report generator that ties every Task 7 piece (benchmarks / stats / regimes / walk-forward / attribution) into a single self-contained dark-theme file with every chart embedded as a base64 PNG (no CDNs, no JS framework). `reports/evaluate.py` exposes `build_report(ReportConfig)` and a `python -m reports.evaluate` CLI (positional strategy + ticker/date/params/commission/slippage/walk-forward/n-resamples flags) that auto-introspects the first 5 tickers from ClickHouse when `--tickers` is omitted, defaults output to `reports/output/<strategy>_<ts>.html`, and degrades gracefully when SPY/^VIX/walk-forward sub-steps fail (regime + attribution sections render a "skipped — backfill X" lead instead of crashing). `reports/charts.py` provides the four chart helpers (equity overlay, price+markers, regime Sharpe bars, walk-forward decay scatter) styled to the RUNBOOK palette via the same `:root` tokens. `reports/overfit_demo.py` (`python -m reports.overfit_demo`) runs a deliberately wide sweep (72 combos on ma_crossover), promotes the IS winner, renders the gauntlet on it, and patches the significance block with the real `deflated_sharpe_ratio_from_sweep` numbers — prints `✅ overfit gauntlet works` when DSR < 0.95, the BRIEF-specified warning otherwise. README §7 documents both entry points and the honest-numbers placeholder table. Tests: 4 new under `tests/reports/` (smoke render, walk-forward render, KeyError on bad strategy name, chart magic-byte assertions, overfit demo against zero-drift noise → DSR < 0.95). CLI smoke check ran end-to-end against the populated ClickHouse for both `evaluate` and `overfit_demo`. Full suite 84 passed + 1 skipped (was 77 + 1).
- Commit: e32d084
- Cut for scope: single-config DSR collapses to PSR in the standard report (the overfit demo carries the real n_trials/σ via a post-render HTML patch — fragile if the template changes); price chart shows the first ticker only (per BRIEF); equity-curve benchmark alignment uses `method="nearest"` rather than a full outer join; no DSR-distribution histogram chart; no PDF export; README "honest numbers" table is a placeholder until Task 4 ships and a maintainer runs the gauntlet by hand. All logged in IMPROVEMENTS.md (Reports).

## 2026-06-25T21:30:00Z — Task 2a [success]
- Summary: Surveyed arxiv (q-fin.TR / q-fin.PM / q-fin.ST, submissions late-2023 through June 2026) for systematic equity strategies with reproducible price/volume signals and reported OOS Sharpe. Skimmed ~250 abstracts across 8 targeted query combinations, applied the BRIEF's (a) implementable-from-public-data / (b) reproducible-signal / (c) realistic-costs filters, and shortlisted 11 candidates with verified arxiv links and per-paper signal descriptions, Sharpe quotes, and implementability scores (1–5 rubric defined inline). Picked Top-3 for 2b using `impl × min(Sharpe, 3.0)` (cap stops the 13-Sharpe paper from trivially winning while still ranking it appropriately as a falsification target). Wrote `research/arxiv_survey.md` (208 lines) + `research/__init__.py`. Added 7 IMPROVEMENTS entries under Strategies tagging the candidates cut for scope (option-data, custom estimators, LLM-dependent baselines, intraday gaps). No implementation work performed per the capacity rule.
- Commit: 249a4ad
- Cut for scope: option-implied p-index (needs option chains), Sparse Jump Model factor regimes (custom estimator), HMM+RL allocation (RL training loop), multi-factor + analyst recs (analyst-rec data dependency), LLM-augmented strategies (Anic, Huang, Fatouros — base strategy not separable from LLM), tick-level intraday papers (Vlasiuk, Mesfin, Lis — outside daily-bar data layer), graph-based stat-arb (Huang et al. — headline is LLM filter not the price baseline). All in `IMPROVEMENTS.md` under Strategies and in `research/arxiv_survey.md` Excluded section.
- Top-3 picked for 2b:
  1. Statistical Arbitrage in Polish Equities (PCA baseline adapted to US universe) — [arxiv:2512.02037](https://arxiv.org/abs/2512.02037) — classical Avellaneda–Lee residual stat-arb.
  2. Continuous Timing Signals for Growth-Defensive Style Allocation — [arxiv:2605.20636](https://arxiv.org/abs/2605.20636) — three macro signals (rates, drawdown, VIX-stress) on two ETF baskets.
  3. Discovery of a 13-Sharpe OOS Factor: Drift Regimes — [arxiv:2511.12490](https://arxiv.org/abs/2511.12490) — picked as a falsification target for the Task 7 PSR/DSR/walk-forward gauntlet.

## 2026-06-25T20:55:00Z — Task 2b [success]
- Summary: Implemented the top-3 arxiv picks from Task 2a as concrete `Strategy` subclasses, registered them, drove them through the existing PSR/DSR/bootstrap-CI/walk-forward gauntlet, and wrote an honest reproduction doc. Strategies: `strategies/pca_stat_arb.py` (Avellaneda-Lee PCA residual stat-arb per Adamczyk & Dąbrowski 2025 §3.1 + eq. 2.10, defaults: window=252, n_factors=15, entry_z=1.25, exit_z=0.5; engine-constrained to long-only); `strategies/macro_timing.py` (Xiong 2026 smooth-score growth/defensive allocator, four direction-normalised macro signals through softplus + tanh + EWMA, paper-selected config eq. 31 defaults; ^IRX substitutes for FRED FEDFUNDS); `strategies/drift_regime.py` (Singha 2025 BASE = 0.7·value + 0.3·reversal gated by UpFraction_63 > 0.60, long-only top-decile selector — picked as falsification target). All three register in `REGISTRY` and the React UI / `/strategies` endpoint pick them up automatically. Tests `tests/strategies/test_arxiv_strategies.py` (14 tests, all pass) cover the signal contract (no NaNs, bool dtype, aligned to data['close']), non-trivial firing on synthetic factor-noise frames, and the per-strategy degradation paths (PCA n_tickers<3 → zero, macro missing SPY/^VIX/^IRX → zero, drift in downtrend → zero). Gauntlet output saved to `reports/output/{pca_stat_arb,macro_timing,drift_regime}.html` (gitignored). Honest writeup in `research/arxiv_results.md`. Full pytest suite: 99 passed (was 98+1 skipped).
- Commits: 12b18dc (pca_stat_arb), ef055ed (macro_timing), d28a814 (drift_regime), 7298b90 (arxiv_results.md + pct_change FutureWarning cleanup), this commit (LOOP_LOG + IMPROVEMENTS).
- Verdict per strategy:
  * **PCAStatArb (arxiv:2512.02037)** — *underperformed*. Point Sharpe 0.36 vs paper's 2.63 (PL 2017). CI [-0.68, 1.46] contains 0; sweep-DSR 0.44. Plausible reasons: cross-section too thin (16 names vs paper's 60), US large-caps 2022-2024 is the wrong regime for residual mean-reversion, no short leg.
  * **MacroTimingXiong (arxiv:2605.20636)** — *survived*. Point Sharpe 0.76 (paper's 1.01 inside our CI [0.18, 1.41]), PSR 0.97, sweep-DSR 0.95, walk-forward OOS 0.63 [0.21, 1.04] across 9 folds. The only one to clear DSR > 0.95.
  * **DriftRegimeSingha (arxiv:2511.12490) [FALSIFICATION TARGET]** — *falsified*. Point Sharpe 0.21 vs paper's claimed >13. CI [-0.94, 1.30] straddles zero, PSR 0.64, DSR 0.34. The gauntlet decisively rejects the headline; clean proof point that PSR/DSR/walk-forward catch a real implausible-Sharpe paper, not just synthetic overfit demos.
- Cut for scope: PCA stat-arb uses long-only translation (engine ABC); macro timing uses ^IRX as FEDFUNDS proxy (no FRED loader); drift regime is long-only (paper is market-neutral); macro full backfill 2018-2026 was started but limited yfinance speed forced us to use a yfinance-direct frame for the macro report (CH still missing ^VIX/^IRX/SPY pre-2022 at the writing of this entry — background backfill in flight); PCA reproduction used 16 names because that's what ClickHouse had backfilled in time (paper used 60); drift sweep used a 16-config sub-grid instead of full 54-combo product to keep gauntlet runtime sane. All logged in IMPROVEMENTS.md under Strategies + Data.

## 2026-06-25T19:58:00Z — Task 7d [success]
- Summary: Wired the evaluation suite into the SPA. Backend: added `POST /regimes/split` (request runs the backtest, joins strategy returns to SPY/VIX bars, calls `tag_all` + `split_stats_by_regime`) plus matching schemas + an API test that skips cleanly when ClickHouse/SPY/VIX are unavailable. Frontend: typed client wrappers for `/benchmarks`, `/stats`, `/walkforward`, `/attribution`, `/regimes/split` with matching TS types. `MetricsTable` now fires `/stats` per ticker via `useQueries` (returns derived from each equity curve) and renders a Tailwind `group-hover` "?" badge revealing the 95% CI for total return / Sharpe / max DD. `Main.tsx` rebuilt around a tab bar (Backtest / Sweep / Regimes / Significance / Walk-forward); `App.tsx`'s `RunResult` now carries the originating request so dependent tabs can rerun. New components: `RegimeBreakdown` (TanStack Table + Recharts bar chart, dimension selector), `SignificancePanel` (PSR baseline + DSR via a second `/stats` call with the deflated `sr_benchmark`; sweep mode feeds n_trials/σ; big badge green > 0.95, amber > 0.5, red ≤ 0.5), `OOSDecayChart` (train/test/mode inputs → `/walkforward`, IS-vs-OOS scatter with y=x reference, decay-slope < 0.3 warning). Added 4 Vitest smokes (one per new component), MSW handlers for the three new endpoints. README "Stack" gained an Evaluation UI subsection covering the four new tabs. Full suite: 77 pytest passed + 1 skipped; 5 Vitest files / 7 tests passed; `npm run build` clean.
- Commit: 996455c
- Cut for scope: random-entry null p-value dropped (PSR + DSR only); DSR is two `/stats` round-trips instead of a `dsr` field on `/sweep`; `aggregateReturns` for the significance panel uses equal-weight per-ticker means (no portfolio equity exposed yet); regime tab requires both SPY and ^VIX backfilled (no inline UX for the 422 response body); OOS chart's walk-forward inputs live in-tab rather than as a sidebar sub-section; CI hover is mouse-only (no aria-describedby); smoke tests are happy-path only. All logged in IMPROVEMENTS.md.
