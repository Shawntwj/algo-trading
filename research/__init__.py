"""Research artefacts (literature surveys, design notes).

Non-importable; this package only exists so paths under `research/` show up
as a normal Python namespace if anything ever needs to load fixtures.
"""
