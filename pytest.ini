[pytest]
markers =
    slow: opt-in network / long-running tests (e.g. live SEC EDGAR round-trips). Run with `pytest -m slow`.
    live: requires live broker credentials (Alpaca / IBKR). Skipped in CI; run with `pytest -m live`.
addopts = -m "not slow and not live"
