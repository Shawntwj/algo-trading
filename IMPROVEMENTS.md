# Improvements

Living backlog of things that could be better. Populated incrementally by every
task in the build loop — each entry is a deliberate cut we made for scope, a
known limitation we shipped around, or a follow-up we noticed while reading
the codebase. Each entry has the shape:

> **Title** — why-it-matters (1 line) — effort (S/M/L) — pointer(s).

Sections are ordered Data / Strategies / Backtest / Live / UX / Tests /
Reports / Infra / Repo (matches `BRIEF.md` Task 6). Within each section,
entries are sorted by effort (S → M → L) then alphabetically by title.

This file is a *backlog*; `scripts/audit.py` is the matching *current-state*
snapshot. Run it (`python scripts/audit.py`) any time to see drift — row
counts per ticker, registered strategies, test counts, API routes, frontend
component count, recent HTML reports, and the number of entries per section
in this file.

## Data

- **13F value-unit conversion is `*1000` everywhere** — `data/edgar.py::parse_information_table` multiplies `value` by 1000 to convert to raw USD. SEC's 2023-Q1 rule update changed reporting from "in thousands" to raw dollars (Bug-fix Release 22-1, 2023-04-13); pre-rule and post-rule filings now require different multipliers. Implement a per-`report_date` toggle to stop double-counting post-2023 filings. — S — `data/edgar.py::parse_information_table`.
- **`backfill_universe` with a single-ticker list crashes** with `KeyError: "None of [Index(['timestamp', 'open', 'high', 'low', 'close', 'volume'], dtype='object')] are in the [columns]"`. `yf.download(['SPY'])` returns flat-column DataFrame; the source's MultiIndex assumption only fires for n>=2. Pad with a sentinel like `^GSPC` until fixed. — S — `data/yfinance_source.py::_to_polars`.
- **ClickHouse `max_partitions_per_insert_block = 100` breaks multi-year backfills** — `backfill_universe(['T1','T2',…], '2018-01-01','2026-06-25')` for a 19-ticker / 9-year span hits the partition limit on the first insert. Task 2b worked around by year-by-year loops in the caller. The fix is to chunk the insert inside `_insert` or pre-OPTIMIZE the table. — S — `data/backfill.py::_insert`.
- **Fundamentals fetcher serially hits yfinance.info** — `data/fundamentals.py::fundamentals_batch` is a plain loop. yfinance.info is one HTTP call per ticker, ~1–2 s each; 30-name S&P proxy + 15 picker holdings = ~45 calls per profile. A `concurrent.futures` thread pool (yfinance is I/O-bound) would cut wall-clock 5–10×. — S — `data/fundamentals.py::fundamentals_batch`.
- **No persistent fundamentals cache** — every profile rebuild re-pulls yfinance.info for the full universe. Cache the seven-field row per (ticker, date) in a `data/fundamentals_cache.csv` keyed by `(ticker, as_of_month)` so a same-month re-run is instant. — S — `data/fundamentals.py`.
- **OpenFIGI cache pollutes the CSV with non-US listings** — `_openfigi_lookup` writes back any first-match ticker, including European / Asian listings (e.g. `BBBYEUR`, `0HAV`). These will never trade in our US-only universe but bloat the file. Filter responses by `exchCode` ∈ {"US", "UN", "UQ", "UP", "UA", "UN"} before persisting. — S — `data/cusip_to_ticker.py`.
- **OpenFIGI maps to alphabetical-first ticker for multi-class issuers** — `data/cusip_to_ticker.py::_openfigi_lookup` picks the alphabetical first common-stock match. For BRK that's `BRKA` rather than `BRKB`; for Alphabet either `GOOG` or `GOOGL`. The committed CSV cache pins these, but new CUSIPs get the default. Add a share-class preference list (e.g. prefer voting class) for known issuers. — S — `data/cusip_to_ticker.py::_openfigi_lookup`.
- **SPY ClickHouse backfill at task-start time only covered 2022-01 → 2024-12** (Task 1c artefact). Task 2b extended it back to 2018-01 via the two-ticker workaround above; long-window strategies (10+ years) still need a deliberate SPY+benchmark refresh. — S — `data/backfill.py`, scripts.
- **Investable universe is 10 large-caps — too thin for picker_compare** — Task 3's A-vs-B comparison runs on the 10 names ClickHouse has backfilled. Most picker holdings (BAC/AXP/KO/CVX for Berkshire; LOW/CMG/HLT for Ackman; BABA/JD/PDD for Tepper/Burry) aren't in the universe, so Literal13FFollow (Variant B) is degenerate — frequently holding only AAPL. A 100+ name S&P 500 backfill would re-run cleanly and tighten the A-vs-B gap reading substantially. — M — `scripts/backfill.py`, `data/queries.py`.
- **No FRED data source** — Xiong (2026) macro timing wants FEDFUNDS (or TNX) as the rate input; we proxied with ^IRX (13-wk T-bill). A `pandas_datareader.get_data_fred` adapter wrapped in the same DataSource interface would close this and unlock any macro strategy that needs published rate series. — M — `data/`.
- **No intraday / minute-bar data layer** — every DataSource implementation today is daily-bar only. Several arxiv candidates and any spread-aware execution sit behind an intraday ingest path (likely Alpaca's IEX feed for free tier, or Polygon for tick data). — L — `data/`, `config/settings.py::Settings.intervals`.

## Strategies

- **arxiv survey cut: graph-based stat-arb (Huang et al. 2026)** — the paper's headline is the LLM-filtered edge graph; the price-only baseline (Sharpe 0.742) is implementable but is a different claim than the paper makes. Use as a benchmark, not a winner candidate. — S — `research/arxiv_survey.md` candidate 3 / [arxiv:2604.19476](https://arxiv.org/abs/2604.19476).
- **CombinedExplainableStrategy weight-learning is opt-in** — defaults to equal weights so `run_backtest` is cheap. Production usage should call `strategy.fit_weights_walk_forward(...)` before backtesting, but nothing in the harness forces that. A `--auto-fit-weights` flag on the gauntlet runner + an opt-in env var would close this. — S — `strategies/combined_explainable.py::fit_weights_walk_forward`, `reports/evaluate.py`.
- **DriftRegimeSingha falsification used a 16-config sub-grid for DSR** — the full `param_grid()` cartesian product is 3×3×3×2 = 54 configs; we ran 16 to keep gauntlet runtime manageable. A full-grid sweep would deflate the point Sharpe even harder and tighten the falsification. — S — `strategies/drift_regime.py::param_grid`, `backtest/sweep.py`.
- **MacroTimingXiong target weight is recomputed every `generate_signals` call** — fine for one-shot evaluation, wasteful when the sweep harness re-runs the strategy many times on the same prices_wide. Memoise on `(prices_wide.shape, hash of params that affect weights)` so the sweep's 8 configs share the macro-input z-scoring. — S — `strategies/macro_timing.py`.
- **PickerCloneStrategy equal-weights the basket** — real picker portfolios value-weight by conviction (Berkshire is 40%+ AAPL). The clone gives every top-N name 1/N weight. A weight derived from similarity score (softmax over cosine sims) would mirror the picker's conviction more honestly. — S — `strategies/picker_clone.py`.
- **PickerCloneStrategy is long-only** — same engine constraint as the arxiv strategies. Picker shorts / activist option positions are ignored. Lift after the Strategy ABC long-short upgrade lands. — S — `strategies/picker_clone.py`, see "Strategy ABC has no first-class long-short" entry below.
- **Picker profiles use a 30-name S&P 500 proxy, not the full 500** — keeps the build deterministic and <2 min per picker but the z-score normalisation could differ at the tail. For factor *diff* (rather than absolute z), 30 names is statistically fine for the seven moments; documented in `research/pickers.md`. Re-run against the full SP500 universe when the backfill is broader. — S — `research/picker_profiles.py::SP500_PROXY`.
- **arxiv survey cut: HMM+RL multi-asset allocation (Verma et al. 2026)** — three-asset universe + RL training loop = high degrees of freedom for small claimed gain; no headline Sharpe in abstract. Worth revisiting once we have a clean PPO scaffold. — M — `research/arxiv_survey.md` candidate 10 / [arxiv:2605.27848](https://arxiv.org/abs/2605.27848).
- **arxiv survey cut: multi-factor + analyst-recommendation strategy (Gkolemis et al. 2024)** — analyst-recommendation series is the soft dependency; pure-price-+-fundamentals ablation is implementable but not reported by the paper. Could be revisited as a "build the implementable subset and own the Sharpe gap" exercise. — M — `research/arxiv_survey.md` candidate 7 / [arxiv:2412.12350](https://arxiv.org/abs/2412.12350).
- **arxiv survey cut: Sparse Jump Model factor regimes (Shu & Mulvey 2024)** — sparse-jump-model is a custom estimator not in scikit-learn out of the box; implementation lift outweighed claimed IR ~0.40. Worth revisiting if Task 2b's regime-allocation pick (Xiong 2026) over-delivers and we want a heavier-weight alternative. — M — `research/arxiv_survey.md` candidate 6 / [arxiv:2410.14841](https://arxiv.org/abs/2410.14841).
- **Bridgewater (Dalio) profile cut for scope** — 700+ position broad-beta book; averaging that many holdings flattens the profile into something indistinguishable from the SP500 mean. Different picker style (risk-parity / macro overlays). Ship as a fifth picker once we add macro / cross-asset features to the factor vector (currency, rate, commodity exposure). — M — `research/picker_profiles.py`, `data/fundamentals.py`.
- **CombinedExplainableStrategy normalises on long-state {0, 1}, not the child's raw signal strength** — when a child fires it contributes z=+1.xx; when it doesn't it contributes z=−mean/σ. This is faithful to the boolean ABC contract but throws away the magnitude information PCA stat-arb and macro_timing carry internally. Once `Signals` grows a continuous-weight field (see the long-short ABC upgrade), wire the raw signal directly into the z-score for a smoother contribution. — M — `strategies/combined_explainable.py::_child_state`.
- **CombinedExplainableStrategy picker-clone child is silent on our universe** — `picker_clone_appaloosa` references BABA / JD / PDD / FXI / KWEB which aren't backfilled in ClickHouse. The child runs cleanly (it's `min_active_children`-aware) but contributes zero to every combined signal; the explanation summary correctly marks it `[inactive: picker_clone_appaloosa]`. Once the universe broadens (see "Investable universe is 10 large-caps" under Data) the picker child will start voting; the combined strategy will need a re-tune of `min_active_children` at that point. — M — `strategies/combined_explainable.py`, `data/backfill.py`.
- **PCA stat-arb inner OU loop is O(n_bars × n_tickers)** — fine for 16 names but a 500-name S&P-500 reproduction (e.g. faithful Singha 2025 universe) would need vectorising the per-stock AR(1) fit. — M — `strategies/pca_stat_arb.py::generate_signals`.
- **Per-bar explanation log is O(n_trades) wide-format** — `self.explanation_log` is a dict keyed by `(ticker, timestamp)` and stores the full `weights` + `child_signals` payload per entry/exit cell. For a multi-year multi-ticker backtest this can grow to tens of MB. A compact representation (one row per (ts, ticker), separate weight history frame) would let the explainability module reconstruct on demand. — M — `strategies/combined_explainable.py`.
- **PickerCloneStrategy projects to a 3-field price-only subspace at runtime** — the committed profile uses all seven factor fields, but at signal time only `log_market_cap` (proxied by log dollar volume), `momentum_12_1`, and `realised_vol_60d` are computable from OHLCV. We compare on the subspace, which throws away the value/quality/leverage signal the profile actually encodes. Plumb yfinance fundamentals (or a persistent fundamentals cache) into `generate_signals` so the runtime vector matches the profile's seven-field shape. — M — `strategies/picker_clone.py`.
- **Strategy ABC has no first-class long-short / continuous-weight signal** — every arxiv reproduction in Task 2b had to translate to long-only booleans, losing real signal: DriftRegimeSingha drops its short leg; MacroTimingXiong replaces a continuous w_G with a binary basket flip at w_G ≷ 0.5 ± 0.05. Lifting `Signals` to carry an optional per-bar `weights` field (and threading through vectorbt's `from_signals(size=...)` or `from_orders`) would close this. — M — `strategies/base.py`, `backtest/engine.py`.
- **arxiv survey cut for scope: option-implied risk measure (p-index, Xie/Nie/Chang 2026)** — needs end-of-day European put-option implied vols. Would unlock a new signal family (option-derived) but requires an option-data vendor beyond yfinance. Revisit if/when we wire an option-chain ingest. — L — `research/arxiv_survey.md` candidate 8 / [arxiv:2606.08569](https://arxiv.org/abs/2606.08569).
- **arxiv survey: implementability gap for ChatGPT-augmented strategies (Anic et al. 2025)** — these papers do not specify the *base* strategy independently of the LLM enhancement, so the implementable subset is the unmeasured baseline. If we ever add an LLM ingestion path, revisit. — L — `research/arxiv_survey.md` Excluded / [arxiv:2510.26228](https://arxiv.org/abs/2510.26228), [arxiv:2603.14288](https://arxiv.org/abs/2603.14288), [arxiv:2604.17327](https://arxiv.org/abs/2604.17327).
- **arxiv survey: tick-level / intraday signals deferred** — papers operating on minute or tick bars ([arxiv:2511.06177](https://arxiv.org/abs/2511.06177), [arxiv:2605.11423](https://arxiv.org/abs/2605.11423), [arxiv:2602.18912](https://arxiv.org/abs/2602.18912)) are outside our daily-bar data layer. Revisit once `data/` grows an intraday ingest path. — L — `research/arxiv_survey.md` Excluded.

## Backtest

- **`/stats` recomputes three independent bootstrap batches** — Sharpe / max-DD / total-return each loop over fresh resamples. A single resample-once-then-apply-three-metrics path would be ~3× faster and is the standard pattern in `arch` / `scikit-learn`. — S — `api/services.py::run_stats`.
- **`_portfolio_returns` averages tickers equal-weight** — matches `summarize`'s cross-ticker mean, but a real multi-ticker portfolio Sharpe should use vbt's portfolio-level value, not a per-ticker average. Once `BacktestResult` exposes a single composite equity curve this collapses to a one-liner. — S — `backtest/walkforward.py::_portfolio_returns`.
- **`buy_and_hold` ignores entry timing of late-listed tickers** — current impl forward-fills NaNs and assumes every ticker starts at the universe's first bar. If a ticker IPOs mid-window we currently buy phantom shares at its first available price; better to defer that ticker's allocation until its first non-NaN bar and rebalance the remainder. — S — `backtest/benchmarks.py::buy_and_hold`.
- **`buy_and_hold_spy` fetches via the data layer on a miss** — but does so synchronously inside a request. For the FastAPI `/benchmarks` path this can block for several seconds on a cold SPY backfill. Either pre-warm SPY at startup or move the fetch to a background task. — S — `backtest/benchmarks.py::buy_and_hold_spy`, `api/app.py`.
- **`decay_slope` uses plain OLS** — one outlier fold (e.g. a vbt NaN on a degenerate window) can swing the slope. Robust regression (Theil-Sen) or a paired bootstrap CI on the slope would be more defensible as a diagnostic. — S — `backtest/walkforward.py::_ols_slope`.
- **`explain_trades` ignores trade direction** — vbt's records carry a `direction` column for long-short portfolios; we hard-code `long_entry` / `long_exit` because the strategy is long-only. When `Signals` lifts to long-short, read the direction column and emit `short_entry` / `short_exit` accordingly. — S — `backtest/explainability.py::explain_trades`.
- **No `/regimes` endpoint yet** — the regime tags exist server-side but the UI (Task 7d) will have to load + tag client-side or via the existing data endpoints. Expose `POST /regimes` that returns the tag DataFrame for a window. — S — `api/app.py`.
- **PSR uses Bailey's raw 4th-moment kurtosis, not excess** — implemented as `scipy.stats.kurtosis(fisher=False, bias=True)` per the paper. This is correct but easy to get wrong by callers eyeballing the formula in a textbook. A second optional argument `kurtosis_convention='raw'|'excess'` with a deprecation path would help. — S — `backtest/stats.py::probabilistic_sharpe_ratio`.
- **VIX backfill is not auto-triggered** — `buy_and_hold_spy` falls back to YFinance for SPY but `regimes.tag_volatility` callers must backfill `^VIX` themselves. Add a `_ensure_series_in_clickhouse` helper. — S — `data/backfill.py`.
- **Volatility terciles are window-relative, not cross-time-comparable** — a low-vol bar in 2020's input window is not a low-vol bar in 2017's. For cross-period comparisons add an optional `quantiles=` arg using fixed historical quantiles. — S — `backtest/regimes.py::tag_volatility`.
- **Walk-forward "best combo" picker is greedy Sharpe-max** — picks the highest-Sharpe combo on the train fold with no PSR / DSR penalty for the size of the grid. A real production picker should deflate the train Sharpe by the number of trials (DSR — already in `stats.py::deflated_sharpe_ratio_from_sweep`) before promoting. — S — `backtest/walkforward.py::_best_combo`.
- **`random_entry_monte_carlo` Sharpe uses fixed 252 annualisation** — fine for daily bars, wrong for intraday. Rigorous annualisation lands in Task 7b's `stats.py`; remove the duplicate here once it does. — S — `backtest/benchmarks.py`.
- **`random_entry_monte_carlo` uses i.i.d. Bernoulli per (path, bar, ticker)** — this matches *average* exposure but not the *autocorrelation* a real strategy exhibits. Add a block-Bernoulli variant (run lengths drawn from a geometric distribution) so the null distribution's realised vol is closer to a real strategy's. — M — `backtest/benchmarks.py::random_entry_monte_carlo`.
- **`regime_split.exposure` uses a non-zero-return proxy** — true position-time exposure requires the vectorbt portfolio (`asset_value() != 0`). Add an overload that accepts the BacktestResult directly so the regime split can read the real exposure. — S — `backtest/regime_split.py`.
- **`spa_test` ships White's Reality Check, not Hansen SPA** — full Hansen (2005) re-centres the bootstrap distribution at zero for any competitor with negative sample mean, which makes the test strictly more powerful when many competitors are obviously bad. We ship White (2000) — the Reality Check ancestor — with re-centring at the sample mean only (not the Hansen v_l / v_c / v_u three-statistic variant). For typical sweep grids where competitors are roughly comparable the difference is small; for grids with many "noise" competitors SPA would reject more often. — M — `backtest/stats.py::spa_test`.
- **Bootstrap CI is percentile, not BCa** — bias-corrected & accelerated bootstrap (Efron 1987) is closer to nominal coverage for Sharpe at small samples (the percentile method has a known undercoverage gap, which is why `test_sharpe_ci_contains_true_value_most_of_the_time` runs at α=0.01 to get ≥90% coverage in 50 trials). Add a `method="bca"` option. — M — `backtest/stats.py::bootstrap_ci`.
- **Child-signal attribution attributes return, not variance** — contributions sum to the combined return but say nothing about each signal's contribution to portfolio volatility. A risk-decomposed variant (`Σ_i w_i² σ_i² + 2 Σ_{i<j} w_i w_j σ_ij`) would tell the user which signal drove the realised risk. — M — `backtest/attribution.py::child_signal_attribution`.
- **Drawdown thresholds are hand-picked** — `-5% / -15%` for calm/mild/severe is conservative but uncalibrated. After Task 7c walk-forward lands, fit thresholds to historically known regime breaks (2008, 2020, 2022) and document the fit. — M — `backtest/regimes.py`.
- **DSR `_expected_max_iid_normal` ignores correlation between trials** — Bailey & López de Prado (2014) treat sweep trials as independent normals. In a real grid sweep adjacent params produce highly correlated returns; the effective `n_trials` is smaller than `len(grid)`, so DSR over-deflates. Add an "effective N" estimator from the correlation matrix of trial returns. — M — `backtest/stats.py::deflated_sharpe_ratio_from_sweep`.
- **`fit_combined_weights_walk_forward` runs each child as its own backtest per fold** — N children × M folds round-trips through `run_backtest`. For the default 6-child line-up × ~5 folds × ~6y daily bars this is the dominant cost of weight learning. Memoise per (child, train_slice_hash) so a re-fit on the same window is instant. — M — `backtest/walkforward.py::fit_combined_weights_walk_forward`.
- **No HAC standard errors on Sharpe** — the rigorous parametric CI for the Sharpe ratio uses Newey-West / HAC variance-of-Sharpe (Lo 2002, "The Statistics of Sharpe Ratios") rather than a bootstrap. We ship only the stationary bootstrap; for callers who want a closed-form CI under serial correlation, HAC would be cheaper and more transparent. — M — `backtest/stats.py`.
- **No Newey-West / HAC standard errors on alpha t-stat** — `market_attribution` uses plain-OLS residual variance for the SE. Returns are typically autocorrelated at lag 1 (volatility clustering), so the SE is too tight and the t-stat is over-confident. The standard fix is Newey-West with `floor(4 * (n/100)^(2/9))` lags. — M — `backtest/attribution.py::market_attribution`.
- **No multi-factor attribution (Fama-French 3F / 5F / Carhart 4F)** — `market_attribution` is single-factor (CAPM). For a multi-factor decomposition the same OLS machinery extends trivially, but the data layer doesn't have FF factors loaded. Pair a `data.backfill_ff_factors` helper with a `multi_factor_attribution` function. — M — `backtest/attribution.py`.
- **`/walkforward` runs the sweep + per-fold backtest synchronously inside the request** — a 10-fold walk-forward on a 3-strategy × 16-combo grid is several seconds, which exceeds typical UI tolerances. A `POST /walkforward/start` → `GET /walkforward/{job_id}` async pattern (or SSE streaming per fold) is the proper fix. — M — `api/app.py::walkforward_endpoint`.
- **Walk-forward has no purged k-fold (López de Prado AFML §7.4)** — `walk_forward` does a chronological split per fold, but with overlapping `step < test_size` we do not purge the bars that leak information across the train/test boundary. The proper fix is to drop a `purge_bars` buffer either side of the split (≥ the strategy's lookback) and, for combinatorial purged CV, embargo a tail of train bars after each test slice. — M — `backtest/walkforward.py::walk_forward`.

## Live

- **`AlpacaBroker.cancel` returns `True` unconditionally** — `alpaca-py`'s `cancel_order_by_id` returns `None` on success and raises on failure; the ABC contract says `False` on "already terminal". Today we conflate "vendor accepted" with "actually open". A pre-check via `order_status` (or parsing 422 "order not open" responses) would honour the ABC contract. — S — `execution/alpaca.py::cancel`.
- **`IBKRBroker.cash()` `BASE` fallback sums only USD lines** — if the account base currency is anything other than USD and BASE rollup is absent, the cash figure is wrong. Real fix: pull `accountValues()` with explicit base-currency conversion via IBKR's reported FX rate. — S — `execution/ibkr.py::cash`.
- **Live runner uses whole-share targets only** — `_target_position_shares` floors to `int(max_position_usd / last_close)` to dodge fractional-share oddities on IBKR. Alpaca supports fractions natively; a `fractional: bool` per-broker flag (or routing rule) would let Alpaca deploy more cash. — S — `execution/live_runner.py::_target_position_shares`.
- **No alerting on kill or risk-block** — when the kill switch trips or an order is risk-blocked, the only signal is a log line and a ClickHouse row. Slack / email / SNS hook would surface these to a phone. — S — `execution/live_runner.py`.
- **No daily-P&L feed into RiskGate** — `RiskGate.record_pnl` exists but the runner never calls it; the daily-loss cap is dormant in live use. Wire `broker.cash()` + `broker.positions()` market-value sum vs the previous-day snapshot at the top of each iteration. — S — `execution/live_runner.py::run_once`, `execution/risk.py::RiskGate.record_pnl`.
- **Schema migration is one-shot, not versioned** — `execution/migrate.py` re-applies the whole `schema.sql`. Real schema evolution wants a migrations table + ordered files. Add `alembic`-style ordering once the schema grows past 2 tables. — S — `execution/migrate.py`.
- **`smoke` script has no `--cancel` end-to-end loop** — `scripts/broker_smoke.py` exercises `connect/positions/cash/submit` but not the full submit→status→cancel cycle. A `--full` mode would close the manual verification gap. — S — `scripts/broker_smoke.py`.
- **Live runner has no order-status reconciliation loop** — once an order is submitted we write `status='submitted'` and move on. Partial fills, rejects, cancels never update the `orders` row. A background task that polls `broker.order_status(order_id)` for non-terminal rows and UPSERTs via the ReplacingMergeTree would close this. — M — `execution/live_runner.py`.
- **Live runner has no UI** — decisions / orders are queryable in ClickHouse but there's no "Live status" tab in the React SPA. Add a tab showing the most recent N decisions, open orders, kill-switch state, and current positions vs targets. — M — `frontend/src/components/`.
- **Live runner polls; no websocket bars** — `execution/live_runner.py` calls `data.load_bars` from ClickHouse on every iteration. Latency for a 5-minute rebalance is fine but minute-bar or tick strategies need a websocket feed (Alpaca `MarketDataStream`, IBKR `reqMktData`). The runner's iteration boundary would shift to "on bar arrival" rather than "on timer". — M — `execution/live_runner.py::run_loop`.
- **Live runner re-instantiates the strategy every iteration** — `_strategy_for(name)` builds a fresh `CombinedExplainableStrategy()` each call so any walk-forward-learned weights are lost between bars. The runner should accept a pickled / pre-fit strategy or run `fit_weights_walk_forward` itself before entering the loop. — M — `execution/live_runner.py::run_once`.
- **No Alpaca crypto** — adapter assumes equities (`OrderSide.BUY/SELL`, `TimeInForce.DAY`). Crypto needs `GTC` TIF, the crypto trading client, and 24/7 session handling. Out of scope for Task 5a. — M — `execution/alpaca.py`.
- **No bracket / OCO / trailing-stop support** — both adapters cap at `MARKET / LIMIT / STOP / STOP_LIMIT`. IBKR and Alpaca both support brackets natively; we don't expose them because Task 5a kept the order vocabulary minimal. — M — `execution/broker.py::OrderType`.
- **No IBKR market-data subscription wiring** — the adapter never calls `reqMktData` / `reqHistoricalData`; the live runner is expected to pull bars from ClickHouse. If anyone ever wants live IBKR ticks (e.g. for spread-aware execution), a subscription layer with the IBKR pacing limits (max 50 simultaneous, 100 per 10s) is needed. — M — `execution/ibkr.py`.
- **No live-CI integration** — broker adapters ship with VCR cassettes + mocked-`ib_insync` tests; CI never touches a real venue. A nightly job that runs `pytest -m live` against a sandbox Alpaca paper account (and an IB Gateway docker image) would catch SDK upgrades or endpoint drift early. — M — `tests/execution/`.
- **No multi-strategy concurrent execution** — the runner takes a single `--strategy`. Running combined_explainable alongside a pure pca_stat_arb book against the same broker requires either multiple processes (with distinct `client_id`s for IBKR) or a router strategy. — M — `execution/live_runner.py`.
- **No order-status streaming (poll-only)** — `order_status()` is a synchronous lookup. The live runner has to poll on a timer; partial-fill granularity is bounded by the poll interval. Both vendors expose websocket trade-update streams (`alpaca-py`'s `TradingStream`, `ib_insync`'s `orderStatusEvent`) — wiring them would give event-driven fill notification. — M — `execution/alpaca.py::order_status`, `execution/ibkr.py::order_status`.
- **No partial-fill / average-fill-price surface** — `Order` carries no fill metadata, and `order_status` returns only the enum. The live runner can't compute realised slippage without re-querying the vendor for fill prices. Promoting `Order` to a `submit() -> Fill` (or adding a `fills(order_id)` method) is the right shape. — M — `execution/broker.py::Broker`.
- **No rate-limit honouring beyond `ib_insync` defaults** — `ib_insync` self-throttles to IB's pacing rules; we add nothing on top. Alpaca's 200 req/min limit is not enforced — burst order submission could trip a 429 we wrap in a generic `BrokerError`. A token-bucket wrapper around `self._client` (or `tenacity` retry on 429) would close this. — M — `execution/alpaca.py`.
- **No futures / options / FX support** — `IBKRBroker._stock_contract` hard-codes `Stock(symbol, 'SMART', 'USD')` and `AlpacaBroker._build_request` only emits equity order requests. Multi-asset trading requires per-asset contract factories and TIF mapping. — L — `execution/ibkr.py::_stock_contract`, `execution/alpaca.py::_build_request`.

## UX

- **CI hover uses Tailwind `group-hover` only** — no keyboard accessibility; screen-reader users can't surface the CI. Promote to a proper `aria-describedby` tooltip (still avoiding Radix per BRIEF) when accessibility hardens. — S — `frontend/src/components/MetricsTable.tsx::HoverCell`.
- **Click-to-explain falls back silently for non-`combined_explainable` strategies** — we opted for "no affordance at all" (`explainable=false` → markers aren't clickable). A tiny "no explanation available" tooltip on hover would make the missing affordance discoverable instead of invisible. — S — `frontend/src/components/PriceChart.tsx`.
- **Copy-as-Markdown does not gracefully fall back without `navigator.clipboard`** — we silently no-op in tests (jsdom has no clipboard). Real browsers always have it, but a TextArea-based fallback for HTTP (non-secure) contexts would prevent a silent miss for self-hosted deployments. — S — `frontend/src/components/ExplainTab.tsx::CopyButton`.
- **Bundle is one 692 KB chunk** — Vite warns at >500 KB. Recharts + d3 dominate. Code-split with `React.lazy` on the chart components, or `manualChunks` for `recharts` + `@tanstack/*`. — S — `frontend/vite.config.ts`.
- **Dev launcher uses busy-wait polling** — macOS bash 3.2 lacks `wait -n`, so `scripts/dev.sh` polls with `kill -0` every second. Negligible cost, but if a contributor rewrites it in zsh / bash 5 it could go back to `wait -n`. — S — `scripts/dev.sh`.
- **DSR is computed via two `/stats` round-trips** — once with `sr_benchmark=0` for the baseline PSR, once with `sr_benchmark = sr_trials_std * E[max]` for the deflated PSR. A dedicated server-side `dsr` field on `/sweep` (using `deflated_sharpe_ratio_from_sweep`) would be one fewer roundtrip and removes the JS-side `normalInv` / Acklam approximation. — S — `frontend/src/components/SignificancePanel.tsx`, `backtest/stats.py::deflated_sharpe_ratio_from_sweep`.
- **Explain tab doesn't fetch the `/strategies/{name}/explanation_schema`** — Task 4b ships the typed wrapper but the UI doesn't render the schema yet (fields are hard-coded to `ticker / timestamp / direction / weights / child_signals / summary`). Once a second explainable strategy ships, drive the detail panel + table columns off the schema response so they auto-adapt. — S — `frontend/src/components/ExplainTab.tsx`, `frontend/src/api/client.ts::strategyExplanationSchema`.
- **Explain tab is enabled solely on a `combined_explainable` run flag** — we don't actually fetch `/strategies/{name}/explanation_schema` to decide whether to enable the tab. Will need re-thinking when other strategies start opting in. — S — `frontend/src/components/Main.tsx`.
- **Frontend dep tree pinned older than upstream latest** — Vitest pinned to ^2.1.9 (not v4) and jsdom to ^25 because Node 20.11.1 in this env lacks `node:util#styleText` (needed by rolldown / vitest 4) and ESM-only encoding shims used by jsdom 29. Bumping Node to ≥20.19 unlocks both. — S — `frontend/package.json`.
- **Heatmap is plain HTML cells, no interpolation** — `SharpeHeatmap` uses an HSL hue ramp on raw values, no contour smoothing, no axes-aware spacing if grid values are non-uniform. Good enough for 3x3..5x5 grids; for larger grids consider a real Recharts ScatterChart with `Cell` colors and a proper colorbar. — S — `frontend/src/components/SharpeHeatmap.tsx`.
- **Marker direction inference is "exits = long_exit" only** — `PriceChart` tags exits as `long_exit` because the only explainable strategy today is long-only. When CombinedExplainable grows shorts (see "Strategy ABC has no first-class long-short"), the chart needs to read direction off the explanation log rather than guessing from which scatter the marker lives in. — S — `frontend/src/components/PriceChart.tsx`.
- **Marker tooltip is generic Recharts default** — entry/exit triangles share the line's tooltip; no per-marker label distinguishing entry from exit timestamp. Custom `<Tooltip content=…>` would fix this. — S — `frontend/src/components/PriceChart.tsx`.
- **No pagination / size cap on `/sweep`** — large grids (e.g. RSI: 3×4×3 = 36 combos) return a single big JSON blob. Either stream NDJSON or cap result count and expose top-N. — S — `api/app.py::sweep_endpoint`.
- **OOSDecayChart sweep mode reuses the prior sweep grid** — sensible default, but the user can't override `train_size`/`test_size`/`mode` without re-running the chart. Surfacing the sidebar's walk-forward sub-section the BRIEF mentions (vs. the in-tab inputs we shipped) is a larger refactor; current shape is in-tab inputs only. — S — `frontend/src/components/OOSDecayChart.tsx`.
- **Per-ticker metric slice is best-effort** — `api/services.py::_ticker_metrics` falls back to `None` when vectorbt returns a scalar instead of a per-ticker Series. Promote this to use `pf[ticker]` sub-portfolios so single-ticker metrics are always populated. — S — `api/services.py`.
- **PriceChart plots equity, not raw price** — `/backtest` returns `equity_curve` per ticker but no underlying close-price series. Entry/exit triangles (and the new explain markers from Task 4b) are placed on the equity line at the nearest equity timestamp, not at the real entry/exit price. Add a `/price` endpoint (or include `close` in `TickerBacktest`) and switch the line to true close so the markers sit on actual price. — S — `api/services.py::serialize_backtest`, `frontend/src/components/PriceChart.tsx`.
- **Regimes tab requires both `SPY` and `^VIX` backfilled** — endpoint surfaces a 422 with the missing-bars reason, but the UI only logs a generic console error. Inline the response body in the failure message. — S — `frontend/src/components/RegimeBreakdown.tsx`.
- **Sidebar param form is flat** — Task 1b renders one `<input>` per `default_params` key (number-typed when the default is numeric). No validation against `param_grid` ranges, no support for list/enum params, no help text. Promote to a schema-driven form once the strategy registry exposes richer param metadata. — S — `frontend/src/components/Sidebar.tsx`.
- **Significance panel uses an equal-weight mean of per-ticker bar returns** — this is a proxy for the portfolio's bar-over-bar return, not the actual portfolio equity series (which `/backtest` doesn't expose today). Once the backend ships a single aggregate equity curve in `BacktestResponse`, switch to that. — S — `frontend/src/components/SignificancePanel.tsx::aggregateReturns`.
- **Task 7d significance panel drops the random-entry null p-value** — BRIEF asked for "PSR, DSR, p-value vs random-entry null". The first two ship; the random-null p-value would require either a new `/significance/random-null` endpoint or wiring `random_entry_monte_carlo` into the existing `/benchmarks` response. Chose the smaller diff (DSR only) to keep this task within the session cap. — S — `frontend/src/components/SignificancePanel.tsx`, `backtest/benchmarks.py::random_entry_monte_carlo`.
- **Ticker multi-select is a raw checkbox list** — fine for a handful of symbols, but will not scale past ~50. Swap for react-select / combobox with search once the universe grows. — S — `frontend/src/components/Sidebar.tsx`.
- **`/backtest` and `/sweep` reload bars per call** — every request hits ClickHouse + `polars_to_wide`. For the same (tickers, range, interval) used during sweep tuning this is wasteful. Add a small LRU cache keyed on the load args, or an explicit `/load` endpoint returning a token reused across runs. — M — `api/services.py::load_wide`.
- **Frontend TS types are hand-maintained** — `frontend/src/api/types.ts` mirrors `api/schemas.py` by hand. Drift is a real risk once the schemas grow. Add `datamodel-code-generator` (Pydantic → TS via OpenAPI) or `openapi-typescript` against `/openapi.json` and wire it into a `make types` target. — M — `frontend/src/api/types.ts`.
- **No e2e coverage (Playwright)** — only one Vitest smoke test (`Sidebar.test.tsx`) covering data loading + params form render. No coverage for Run-button → mutation, table sorting, sweep flow, or chart rendering. Add MSW-backed component tests and a Playwright run-through against `make dev`. — M — `frontend/src/__tests__/`.

## Tests

- **CombinedExplainable tests don't lock down the actual contribution math** — `test_at_least_one_entry_has_complete_explanation` checks the explanation *shape* (keys, weights sum to 1) but doesn't assert `summary` contains the top-contributor names or that the per-bar weight history matches the strategy's stored weights cell-for-cell. A fixture-driven contribution test against a hand-computed expected value would catch a future bug where the summary line drifts from the underlying numbers. — S — `tests/strategies/test_combined_explainable.py`.
- **EDGAR live-network smoke gated by `pytest.mark.slow`** — `tests/data/test_edgar.py::test_list_13f_filings_berkshire_smoke` only runs with `pytest -m slow`. Default suite gets no coverage of the live SEC round-trip. A nightly cron that runs `-m slow` against the production EDGAR endpoint (and rate-limit-respecting backoff) would catch endpoint or schema drift early. — S — `tests/data/test_edgar.py`.
- **ExplainTab smoke test stops at row-click → select** — it asserts the detail panel + bar chart show up but doesn't drive the Copy-as-Markdown click (jsdom clipboard limitation), doesn't assert the bar chart's bar order matches the sort-by-|w*s| invariant, and doesn't cover the PriceChart marker → ExplainTab cross-component flow (would need Main + App in the harness). — S — `frontend/src/__tests__/ExplainTab.test.tsx`.
- **Live runner ClickHouse tests share a single tmp DB across tests** — `tests/execution/test_live_runner.py` builds one tmp DB at module import and drops it in `teardown_module`. Tests assert `>= 2` rather than `== 2` for the blocked-decisions count because the previous test contributed rows to the same DB. A `tmp_db_per_test` fixture would let exact counts be asserted but doubles ClickHouse churn. — S — `tests/execution/test_live_runner.py`.
- **No fixture-bytes test for the literal-13F holdings_history builder** — `backtest/picker_compare.py::build_holdings_history_live` is exercised only by the live `scripts/run_picker_compare.py` driver, not pytest. A test that feeds in a mocked `edgar.list_13f_filings` + `fetch_13f_holdings` would lock down the date-arithmetic / CUSIP-filter behaviour without needing network. — S — `tests/backtest/test_picker_compare.py`.
- **No frontend tests yet** — Task 1b deferred Vitest/Playwright. A smoke test covering "Sidebar fetches tickers + strategies, Run button posts to `/backtest`" with MSW mocks would catch regressions cheaply. Add in Task 1c alongside the chart work. — S — `frontend/`.
- **No test of the long-running `run_loop`** — `run_loop` is exercised only by manual smoke. A test that spawns the loop in a thread, sets the kill switch, and asserts shutdown happens within `rebalance_seconds + 2` would lock the SIGINT/SIGTERM contract. — S — `tests/execution/test_live_runner.py`.
- **Picker_compare test uses a trivial profile** — `test_picker_compare_returns_gap` builds a zero-vector profile so the cosine sim picks arbitrary winners. A test that constructs a profile favouring a known subset of synthetic tickers and asserts Variant A's basket lands on those names would tighten the contract. — S — `tests/backtest/test_picker_compare.py`.
- **PriceChart click-to-explain has no test** — the new clickable marker + selected-state ring shipped without coverage. A test that renders PriceChart with two markers, clicks one, and asserts `onSelectTrade` fires with the right payload would close the gap. — S — `frontend/src/__tests__/`.
- **Task 7d smoke tests are happy-path only** — each new component has one Vitest with the MSW handler returning canned 200s. No coverage for error states (e.g. `/regimes/split` 422 when SPY is missing), empty-data branches in OOSDecayChart's scatter, or sort interactions on the regime table. — S — `frontend/src/__tests__/`.
- **`/backtest/explain` test is happy-path only and ClickHouse-gated** — happy path covers shape; we don't test the 422 path (universe too small for any child to fire) or the empty-trades path (long enough window but `min_active_children` too high). A synthetic-bars fixture (paired with the picker_clone profile loader bypass) would let these run unconditionally. — S — `tests/api/test_endpoints.py`.
- **API tests skip when ClickHouse is unreachable** — `tests/api/test_endpoints.py` skips `/tickers`, `/backtest`, and `/sweep` tests if `list_tickers()` fails or returns empty, per the BRIEF's no-mock principle. A tmp-schema fixture (insert synthetic bars, run against an isolated DB, drop on teardown) would let these run in CI without a populated `algo.bars`. — M — `tests/api/test_endpoints.py`.
- **Live runner has no end-to-end paper test in CI** — `--once` against Alpaca paper is gated on `ALPACA_API_KEY` being set in the runner's env (BRIEF acceptance test). CI never has those creds. A nightly job running `--once` against a sandbox paper account with rotating creds would close the gap. — M — `tests/execution/`.
- **`/regimes/split` API test is happy-path only** — confirms response shape; doesn't validate that the per-regime numerics match a known synthetic fixture. A small fixture-bars test (seeded SPY/VIX in a tmp ClickHouse schema) would lock down the numerics. — M — `tests/api/test_endpoints.py`.

## Reports

- **`equity_chart` reindexes benchmark series by `method="nearest"`** — when the benchmark calendar drifts from the strategy calendar (e.g. SPY skipping bars the strategy held), nearest-fill papers over the gap. An outer-join + ffill would be more honest. — S — `reports/evaluate.py::_equity_chart`.
- **DSR chart of trial-Sharpe distribution missing** — the report shows the DSR number but not the sweep-Sharpe distribution that produced it. A histogram of `sweep_sharpes` with the winner highlighted would make the deflation visible. — S — `reports/evaluate.py`, `reports/charts.py`.
- **HTML report README baseline is a placeholder template** — the "honest numbers" table in README §7 has placeholder cells; populating it programmatically (e.g. a `make report-readme` target that runs the gauntlet and pastes the numbers back in) would keep it honest. Currently the maintainer has to run the gauntlet and edit by hand. — S — `README.md`.
- **Overfit demo patches the rendered HTML via regex** — significance block is replaced post-render with `re.sub`. Fragile if the template changes (the demo silently no-ops). Better fix: lift the significance computation out of `build_report` and let the demo pass a pre-computed block in as a `ReportConfig` override. — S — `reports/overfit_demo.py::_patch_significance`.
- **Price chart only renders the first ticker** — by design (per BRIEF, "if you do all of them the report explodes"). A future "full" mode could embed each ticker's price chart inside a `<details>` so the report stays reasonable size while letting the reader drill in. — S — `reports/evaluate.py::_price_chart`.
- **Random-entry null uses realised-non-zero-return as exposure proxy** — `_significance_block` clips `(strat_returns != 0).mean()` to `(0.05, 0.95)`. For a always-on long strategy this proxy reads ~99% exposure (every bar has some return) which is fine, but for a flat-most-of-the-time signal it overshoots. Plumb `BacktestResult.portfolio.asset_value()` through to compute true exposure. — S — `reports/evaluate.py::_significance_block`.
- **Single-config DSR collapses to PSR in `reports.evaluate`** — `_significance_block` calls `deflated_sharpe_ratio(n_trials=1, sr_trials_std=0.0)` because a single backtest doesn't carry its sweep. The overfit demo patches the HTML post-render with the real sweep stats; the regular `reports.evaluate` entry point should accept an optional `--sweep-results` JSON path so DSR reflects the actual trial count without needing the demo. — S — `reports/evaluate.py::_significance_block`, `reports/overfit_demo.py::_patch_significance`.
- **No PDF export** — HTML-only. For sharing via email a `weasyprint` pass would produce a one-file PDF, but it adds a heavy dependency. Deferred. — M — `reports/`.

## Infra / Repo

- **`audit.py` is a current-snapshot only — no historical drift graph** — `scripts/audit.py` prints the platform's state *right now*; there is no time-series store of "rows per ticker / test count / # strategies" so we can't see drift across weeks. A nightly cron that appends one JSON row per run to `reports/audit_history.ndjson` (plus a tiny `reports/audit_trend.html` line chart) would close the loop. — S — `scripts/audit.py`.
- **No CI workflow** — no `.github/workflows/` directory. `pytest -q` and `npm --prefix frontend test -- --run` only run on a contributor's laptop; broken `main` is only caught by the next dev to pull. A minimal GitHub Actions matrix (`python 3.11/3.12 × pytest`, `node 20 × vitest`) with a ClickHouse service container would prevent regressions. — S — `.github/`.
- **No coverage instrumentation** — `pytest` is configured for marker-based selection but never reports coverage. A `pytest-cov` dep + `pyproject.toml` config + `coverage report --fail-under=60` in CI would gate test depth. Today we count test *files* and *collected tests* (see `scripts/audit.py::_collect_tests`) without knowing which lines they exercise. — S — `pytest.ini`, `requirements.txt`.
- **No git remote configured** — repo was bootstrapped with a local `git init`; remote push is deferred. Add a remote (`git remote add origin …`) and switch the loop to push per task. — S — `LOOP_LOG.md` bootstrap entry.
- **No LICENSE file** — repo has no top-level licence declaration. Even for a personal research repo, an explicit licence (MIT / Apache-2 / proprietary) clarifies whether the arxiv reproductions can be cited / re-used. — S — repo root.
- **No `Dockerfile` for the FastAPI service** — `docker-compose.yml` only stands up ClickHouse; the API still runs from a host venv. A multi-stage `Dockerfile` (build wheel → slim runtime) plus a `api` service in `docker-compose.yml` would let a fresh checkout `docker compose up` end-to-end. — M — `docker-compose.yml`, `api/`.
- **`requirements.txt` is `>=`-only, no lockfile** — every entry uses a lower bound (`polars>=0.20.0`, `vectorbt>=0.26.0`, …) with no pinning. A breaking upstream release (vectorbt has shifted internals before) can silently rot the suite on a fresh `pip install`. Promote to `pip-tools` (`requirements.in` → `requirements.txt` with hashes) or `uv` lockfiles. — S — `requirements.txt`.
